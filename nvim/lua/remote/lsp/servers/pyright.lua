local M = {}

M.config = {
  settings = {
    python = {
      autoImportCompletions = true,
      autoSearchPaths = true,
      useLibraryCodeForTypes = true,
      -- extraPaths = { "src", "/src" },
    },
  },
}

return M
