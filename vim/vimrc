"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
" => Plugin Manager
"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
call plug#begin('~/.config/vim/remote')

" show completion entries for typed characters
Plug 'vim-scripts/AutoComplPop', { 'on': [] }

" language server protocol support
if has('nvim')
	Plug 'nvim-lua/completion-nvim'
	Plug 'neovim/nvim-lspconfig'
endif

" command line fuzzy finder
Plug 'junegunn/fzf.vim', { 'on': ['Files', 'Buffers', 'Ag', 'Rg', 'Windows'] }

" syntax highlighting for portage
if has('nvim') && has('unix') && isdirectory("/etc/portage")
	Plug 'gentoo/gentoo-syntax'
endif

" distraction-free writing
Plug 'junegunn/goyo.vim'

" hyperfocus-writing
Plug 'junegunn/limelight.vim'

" emphasize the current matched search pattern
Plug 'wincent/loupe'

" code formatter
Plug 'prettier/vim-prettier', {
  \ 'do': 'yarn install',
  \ 'for': ['javascript', 'typescript', 'css', 'less', 'scss', 'json', 'yaml', 'html'] }

" easily identify nested parentheses
Plug 'junegunn/rainbow_parentheses.vim'

" undo history visualizer
Plug 'mbbill/undotree', { 'on': 'UndotreeToggle' }

" file-type aware comments
Plug 'tpope/vim-commentary'

" create file and parent direcory at the same time
Plug 'duggiefresh/vim-easydir'

" a git wrapper for vim
Plug 'tpope/vim-fugitive'

" highlight the yanked region
if !exists('##TextYankPost')
	Plug 'machakann/vim-highlightedyank'
else
	Plug 'machakann/vim-highlightedyank', { 'on': [] }
endif

" highlight sets of matching text
Plug 'andymass/vim-matchup'

" enable repeating supported plug maps with <.>
Plug 'tpope/vim-repeat'

" display git changes in gutter
Plug 'mhinz/vim-signify'

" surround sequence with tags
Plug 'tpope/vim-surround'

" enable focus events
Plug 'tmux-plugins/vim-tmux-focus-events'

" dim the current window/buffer when not active
Plug 'TaDaa/vimade'

" maintain git workflow within vim
Plug 'jreybert/vimagit'

call plug#end()
